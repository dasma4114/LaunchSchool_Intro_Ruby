names = ['bob', 'joe', 'susan', 'margaret']
#names['margaret'] = 'jody' => this produces an error because we're trying to
#access an element of the array with it's key. 

#accessing the element with its index will let us replace the value of that element.
names[3] = 'jody'

print names